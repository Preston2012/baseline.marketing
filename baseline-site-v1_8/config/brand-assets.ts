/* ─────────────────────────────────────────────────────────
   Brand Assets - Static file references
   ─────────────────────────────────────────────────────────
   All assets served from /public/brand/ for optimal caching
   and Next.js Image optimization. Original files available
   for press downloads and JSON-LD schema references.

   Wordmark + ba_mark have WebP versions for in-app use
   (12KB total vs 38KB for the PNGs). PNGs retained at the
   same path for press downloads + JSON-LD schema where
   universal compatibility matters more than file size.
   ───────────────────────────────────────────────────────── */

export const BRAND_WORDMARK = '/brand/wordmark.webp';

export const BRAND_BA_MARK = '/brand/ba_mark.webp';

export const BADGE_APPSTORE = '/brand/badge_appstore.svg';

export const BADGE_GOOGLEPLAY = '/brand/badge_googleplay.svg';
